# SRD Markdown Content

This folder is for a local snapshot of markdown resources from:

- `../dnd-24-resources`

Use:

```bash
./scripts/sync-srd-content.sh
```

to copy the latest `chapters/`, combined markdown, and attribution file into this folder.
